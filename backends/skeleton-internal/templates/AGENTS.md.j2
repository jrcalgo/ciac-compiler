A generated `skeleton-internal` project has no compiler-owned/seeded
split yet — everything here is a fixed demonstration file. A real
backend's `AGENTS.md` (see `ciac-backend-python`/`-rust`'s) explains
that split for real.
