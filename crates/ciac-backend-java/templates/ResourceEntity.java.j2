// {{ resource.name }}Entity.java -- Generated by CIaC.
//
// The generic keyed-document row shape for the untyped `crud
// {{ resource.name }};` resource -- a server-generated id plus
// whatever JSON the client posted under `data`. Named `<Name>Entity`
// (not `<Name>`) so a resource sharing a name with an unrelated wire
// `record` never collides in the `schemas` package.
package com.ciac.{{ java_package }}.schemas;

import com.fasterxml.jackson.databind.JsonNode;

public record {{ resource.name }}Entity(String id, JsonNode data) {}
