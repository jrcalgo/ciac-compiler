//! {{ c.service_name }} — generated by CIaC.
//!
//! Compiler-owned wiring lives at the crate root and in `routes`/`workers`;
//! business logic belongs in `services`, which is yours.

pub mod config;
pub mod error;
pub mod observability;
pub mod routes;
pub mod state;
{%- if c.has_auth %}
pub mod auth;
{%- endif %}
{%- if c.resources %}
pub mod db;
pub mod models;
{%- endif %}
{%- if c.services or c.resources %}
pub mod services;
{%- endif %}
{%- if c.workers or c.consumers %}
pub mod workers;
{%- endif %}
