//! Persistence models and API schemas. Generated by CIaC.
//!
//! Each `crud` resource gets a generic keyed-document model: a UUID
//! primary key plus a JSON payload. Tighten these into real columns as
//! your domain model settles.

use serde::{Deserialize, Serialize};

{% for resource in c.resources %}
#[derive(Debug, Clone, Serialize, sqlx::FromRow)]
pub struct {{ resource.name }} {
    pub id: String,
    pub data: serde_json::Value,
}

/// Payload for creating or updating a {{ resource.name }}.
#[derive(Debug, Clone, Deserialize)]
pub struct {{ resource.name }}In {
    #[serde(default)]
    pub data: serde_json::Value,
}
{% endfor %}
