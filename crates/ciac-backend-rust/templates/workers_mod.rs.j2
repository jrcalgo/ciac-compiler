//! Queue-driven workers. Generated by CIaC.
//!
//! Run them all with `cargo run --bin workers`.

{% for worker in c.workers -%}
pub mod {{ worker.snake }};
{% endfor -%}
{% for job in c.jobs -%}
pub mod {{ job.snake }};
{% endfor -%}
{% for consumer in c.consumers -%}
pub mod {{ consumer.snake }};
{% endfor -%}
