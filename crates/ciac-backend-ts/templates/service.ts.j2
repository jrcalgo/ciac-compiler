// Business logic for the `{{ service.class_name }}` pipeline step.
// Generated by CIaC.
//
// This file is yours: the compiler wires `{{ service.class_name }}`
// into its pipelines, and you implement the business logic in
// `handle`. Regeneration never overwrites your edits here.

import type { AppState } from "../state.js";
{%- if service.payload %}
import type { {{ service.payload.class_name }} } from "../schemas.js";
{%- endif %}

{%- set payload_type = service.payload.class_name if service.payload else "unknown" %}

export class {{ service.class_name }} {
  constructor(private readonly state: AppState) {}

  async handle(payload: {{ payload_type }}): Promise<{{ payload_type }}> {
    // TODO: implement {{ service.class_name }}.
    return payload;
  }
}
