# Dogfooding session feedback log

> Copy this file per session (e.g. `docs/dogfooding/sessions/
> 2026-08-01.md`) and fill it in live, per the script in
> [`DOGFOODING.md`](../../DOGFOODING.md). One observation per line,
> tagged `{friction|concept|bug|want}` — see that file's "Capturing"
> section for what each tag means and which issue template to file it
> under.

## Session info

- Date:
- Tester background (role, prior exposure to CIaC — should be "none"):
- Observer:
- Environment (fresh VM / container / other):

## Phase 1 — cold start (0:00–0:20)

Where did they land first? What did they read in full vs. skim?

- [ ] `friction` / `concept` / `bug` / `want` —

## Phase 2 — guided build (0:20–0:50)

Every place they stopped, re-read, or typed something the guide
didn't say.

- [ ] `friction` / `concept` / `bug` / `want` —

## Phase 3 — the hook (0:50–1:10)

Did they find `ciac sim` unprompted? Did the scenario format make
sense without extra explanation?

- [ ] `friction` / `concept` / `bug` / `want` —

## Debrief

1. What is this tool, in your words?
   >
2. What almost made you quit?
   >
3. What surprised you, good or bad?
   >
4. Would you use it? For what? Why not?
   >
5. What did you want that wasn't there?
   >

## Issues filed from this session

| Line item | Tag | Issue |
| --- | --- | --- |
| | | |
