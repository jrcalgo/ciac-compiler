// expect: CIAC0020
service X;
record A { id: Uuid; status: enum { Ready, Failed }; }
api A: A;
pipeline A:
    match status {
        Ready -> Return;
        Other -> Return;
        _ -> Return;
    };
